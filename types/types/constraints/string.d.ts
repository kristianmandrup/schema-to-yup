export function createStringConstraint(typer: any, map: any): StringConstraint;
export class StringConstraint extends Constraint {
    constructor(typeHandler: any, map?: {});
    isValidConstraint(value: any): any;
}
import { Constraint } from "./base";
